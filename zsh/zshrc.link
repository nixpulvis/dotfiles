#=>~/.zshrc
# This file is just here to source other files, that actually do work,
# it's super lazy and should move out of it's mother's basement.

export DOTFILES=$HOME/.dotfiles

# Source .zsh! priority files.
for priority ($DOTFILES/**/*.zsh!) echo $priority; source $priority

# Source all .zsh files.
for source ($DOTFILES/**/*.zsh) echo $source; source $source