window:
  # Using dynamic padding alone results in a small padding
  # area, which changes in size.
  dynamic_padding: true

  # Setting this to "none" will result in window without borders and title bar.
  decorations: "full"


scrolling:
  # How many lines of scrollback to keep,
  history: 10000

  # Number of lines the viewport will move for every line scrolled.
  multiplier: 3

# Font configuration (changes require restart)
font:

  # xrandr:
  # 310mm x 170mm
  # 1920x1080
  #
  # size: 18  # My eyes!
  size: 10     # Glasses are nice.

  normal:
    family: "Fira Mono"

# cursor:
#   style:
#     shape: Block
#     blinking: true

import:
  - '/home/nixpulvis/.config/alacritty/bindings.yml'
  - '/home/nixpulvis/.config/alacritty/colors.yml'
